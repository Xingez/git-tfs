The unit tests for git-tfs use MSTest.

From the `src` directory, run the test suite with the .NET CLI:

```powershell
dotnet test .\GitTfsTest\GitTfsTest.csproj
```

The tests can also be run from Visual Studio 2022's Test Explorer.
